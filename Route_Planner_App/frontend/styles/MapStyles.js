import { StyleSheet } from 'react-native';

const MapStyles = StyleSheet.create({
    container: {
        flex: 1,
        width: "100%",
    },
    map: {
        width: '100%',
        height: '100%',
    },
});export default MapStyles;

